# AUTOSAR 迁移验收状态

此表记录代码实际接入状态，不把创建目录或注册空组件视为完成。多账号 TODO 按用户要求排除在本轮范围之外；其他未完成项仍需实现，不能据此宣称迁移全部完成。

| 范围 | 状态 | 剩余验收 |
| --- | --- | --- |
| 单 Go 模块、原 core 可见性 | 已完成 | 原 clone 转发依赖的下载/上传实现保留 |
| RTE 端口装配和生命周期 | Bot、aria2、面板、HTTP 和监听适配器的启停由 RTE Process 管理；超时保留旧实例并拒绝重叠启动；重启先等待旧实例退出 | app/runtime 的配置协调和完整依赖图装配仍需迁移 |
| 事件与 Runnable | 表情下载、表情转发、监听新消息转发及普通通知接入有界 RTE 事件；转发消费退出后才释放连接；面板同步、Range 清理和 aria2 治理纳入周期任务 | 其余连接任务全面接入 |
| filter.rules / naming.rules | 生产宿主统一持有生命周期和批量配置，监听器注入端口；独立入口保留自有宿主 | 随整体宿主迁移去除兼容配置适配 |
| trigger.messagelink | 主宿主持有校验与提交编排端口；协议源获取消息后经有界 download.requests 发布意图并返回关联结果；取消、停止及处理器 panic 均不会挂起请求；Bot 预校验不再创建临时宿主 | 当前迁移项已完成 |
| trigger.reaction | 匹配/去重端口、独立配置和热更新已接入；下载与转发意图分别由 trigger.download、trigger.forward 发布和消费，保持背压及取消行为；监听连接由单账号共享资源所有者管理 | 当前迁移项已完成 |
| update.self | 配置、Bot/WebUI 更新端口、停机等待及导航已接入；页面资源和 API 路由声明归更新组件 | 当前迁移项已完成；真实更新替换保留发布环境验收 |
| taskhub / NvM | 四类任务写入统一；协议数据经受限 NvM 数据集；本地和 aria2 记录持久化规范 state/revision；终态禁止回退；aria2 报告按读取版本比较更新，迟到结果不能覆盖控制操作或复活删除记录；链接与远端关联事务删除兼容无索引旧记录；维护适配器过滤协议密钥并比较快照删除；目录快照仅输出链接与 aria2 记录，排除索引和其他数据集 | 剩余零散数据集及面板重试关联发现、同步适配核对 |
| aria2 RPC | 类型化客户端及 RPC 测试迁入 BSW aria2rpc；AriaNg 页面及路由声明归 downloader.aria2，统一鉴权后由 HTTP 适配器绑定 | 当前迁移项已完成 |
| 配置持久化 | 八个静态组件及本地下载器、aria2 治理、转发队列、Range、下载路由接入生产文件和 schema 页面；动态配置热更新并重启恢复；敏感配置分离；旧配置页拒绝覆盖已迁移字段；显式本地降级路径独立配置 | 其他业务组件配置、共享配额配置归属、完全移除兼容配置 |
| account.telegram / tgauth | 主宿主持有凭据及共享连接；面板与 Bot 登录状态机均归账号组件，通过纯数据认证/消息端口调用协议适配器；Bot 登录由 RTE 管理并等待流程退出；AccountSession 统一生产会话检查且显式依赖账号资源；SessionCatalog 管理会话列表与删除规则，删除与登录互斥；临时认证隔离，替换等待旧传输退出 | 单账号登录、检查与会话管理端口已接入；真实账号端到端验收待执行 |
| proxy.range / comif | Range 业务及清理归 SWC；账号传输资源共享；HTTP 停机等待请求与清理；等待连接、保存记录超时及两类清理周期支持独立持久配置和热更新；动态周期保留诊断和失败后继续调度；共享配额支持热更新 | HTTP 监听地址、链接 TTL 与共享配额的独立配置归属 |
| downloader.local / downloader.aria2 | 执行与控制归 SWC，仓库归 taskhub；download.control 持久化执行器优先级与独立本地根目录，明确拒绝才可跨执行器降级；本地路径与远端路径分别准备；持久状态转换及 aria2 独立状态同步已接入；本地扫描/停机超时支持组件配置；aria2 状态同步、重连退避、零速检测与 Telegram 错误治理的 12 项参数支持持久化、热更新和重启恢复 | aria2 RPC 地址、凭据及其余兼容配置归属，以及真实后端端到端验收 |
| forwarder | 队列、控制、恢复及持久化已迁入；触发经有界事件；扫描间隔、重试预算、退避与历史保留策略接入生产配置文件、热更新和重启恢复；页面及路由归组件 | 并入统一生产装配；监听目标等触发配置归属 |
| console.bot / notify.telegram | console 管理菜单、私聊限制和权限；notify 管理通知及进度；登录命令经 BotLogin 端口进入账号组件，消息入口至密码处理均保留原文；KV 清理经 KVMaintenance 端口由 storage.maintenance 编排，RTE 停机取消并等待清理退出 | 其余命令编排核对、整体生命周期编排 |
| panel.webui / hmiif | HTTP 生命周期、配置修改规则和脱敏归 panel SWC；Configuration 端口保存时比较旧快照，拒绝覆盖其他入口的新值；配置 DTO 归 interfaces，旧 pkg/config 保留兼容别名；登录、会话检查、列表和删除均通过账号端口；DownloadLinks 管理链接删除；DownloadCatalog 接管目录展示、滑动过期、完成判定与批量提交，去重并拒绝 ID 不一致记录，保留远端已接受但持久化失败的结果；页面资源分组件，路由统一鉴权 | 重试关联发现与状态同步适配、账号切换及其他剩余处理器端口化；新页视觉验收 |
| 诊断、监控、发布与文档 | 动态适配器和 SWC 纳入诊断；RTE 提供有预算的恢复，Bot 对网络错误最多恢复 3 次；配置文档 CI 检查及共享构建参数已接入，配置参考增加本地下载器实际 schema | 其余组件恢复策略和动态配置参考、跨平台发布及迁移交付验收 |

优先继续处理生产宿主和端口契约，再迁移账号与下载链路。每次迁移保留旧数据兼容和业务回归，完成后再移除适配层。

2026-09-17 核对：八个已注册组件的生产配置接入已补齐，但不等于整体 AUTOSAR 迁移完成。六个常驻组件为 account、filter、naming、两个 trigger 和 update；console/notify 仍随 Bot 运行。完整业务编排、下载器/转发/Range SWC、统一任务状态和多账号并行仍未完成。

2026-09-18 续迁：转发业务已拆为独立 SWC，连接宿主显式绑定持久队列及传输，不注册没有资源的占位组件；默认 `swc-check` 仍检查八个静态组件。转发停止等待实际调用退出后才交还连接；发送及相册读取错误不再吞掉，删除后进度/完成回调不得重建记录，人工重试重置次数。peer、更新偏移及 access hash 已持久化，并覆盖跨句柄事务和账号隔离；KV 清理保留这些协议数据。共享下载配额支持在线调整。整体编排、下载器/Range 完整 SWC、触发意图、账号连接/登录所有权、面板导航、统一任务状态及多账号仍需继续实现，不能将本次进展当作全部任务完成。

2026-09-18 后续进展：本地下载器、aria2 下载器与 Range 业务已迁入实际使用的 SWC，替代上段对应的拆分待办。下载仓库和类型化 RPC 已迁入 BSW；Bot 的下载控制使用公共端口。新增回归覆盖重复提交保留原状态、跨句柄事务更新、删除/暂停后的迟到报告、短流不误报完成，以及停机超时后保留资源并再次等待退出。监听器退出先等待分发器停止提交，再等待任务和更新管理器结束。静态配置参考由 `swc-docs` 自动生成，多账号验收拆解见 `TODO-multi-account.md`。表中其余验收项仍未完成。
