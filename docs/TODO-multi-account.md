# 多账号后续阶段

原计划将并行多账号列为 M6。本文件保存验收项，不将已有的账号参数、namespace 存储隔离或单账号运行能力视为多账号并行完成。

| 项目 | 当前状态 | 验收要求 |
| --- | --- | --- |
| M1 账号运行时 | 待完成 | 两个账号同时持有独立会话、连接及 DC 池；停止一个不影响另一个 |
| M2 配额 | 待完成 | 配额按账号和 DC 隔离，保留公平性及取消后租约回收 |
| M3 RTE 实例 | 待完成 | 按账号装配组件，诊断和状态使用实例标识 |
| M4 实例生命周期 | 待完成 | 单个账号登录失败、停止和重启不影响其他实例 |
| M5 配置实例 | 待完成 | 默认配置与账号覆盖按明确优先级合并，敏感值独立保存 |
| M6 Bot 控制 | 待完成 | 命令显式选择账号，默认账号规则明确，回复标明归属 |
| M7 面板 | 待完成 | 账号切换器和任务分组，服务端拒绝越权跨账号操作 |
| M8 存储路由 | 已有 namespace 隔离，待并行验收 | 同时操作两个账号的任务、协议状态和会话，无串写或清理误删 |

实施顺序：先完成账号资源唯一所有权，再实现 M1/M2 和 M3/M4，最后接入配置、控制面及双账号端到端验收。
