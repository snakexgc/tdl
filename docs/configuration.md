# TDL 统一配置参考

由 `go run ./cmd/swc-docs` 根据已接入配置存储的组件 Manifest 生成。不要手工修改字段表。

正常启动只使用应用目录的 `tdl_config.json`。`configuration.manager` SWC 集中负责全局配置的默认值补全、校验、版本检查和原子保存；业务组件通过 RTE 获取各自的配置视图。完整默认模板见 [examples/tdl_config.json](../examples/tdl_config.json)。模板由 `go run ./cmd/swc-docs -config-out examples/tdl_config.json` 生成，不含用户密钥。

本表覆盖 18 个业务组件及统一配置管理 SWC。字段 schema 由业务组件声明，配置管理 SWC 通过统一 Catalog 收集；不再各自写配置文件。缺组件或缺字段使用 schema 默认值，不回退旧配置；未知组件、字段、错误类型或无效组合会阻止加载，停用组件同样校验。

## 文件结构与系统设置

`version` 当前为 `1`。`system.namespace` 默认 `default`，只允许英文字母，选择启动时使用的登录会话与历史任务；WebUI 通过账号管理切换并重启进程，同一进程只运行一个 Telegram 账号。`system.debug` 默认 `false`，控制详细日志，保存并手动重启后生效。`components` 的键为下面列出的组件 ID，每个组件由 `enabled` 和 `values` 组成。`trigger.forward` 默认停用，其余组件默认启用；配置管理 SWC 固定启用，不需要在 components 下声明。所有设置全局共用，切换命名空间不会重置端口、密码、代理或其他组件设置。

手工编辑的路径示例：`components["downloader.aria2"].values.monitor_stall_seconds`。组件 ID 中的点属于键名，不代表嵌套对象。JSON 不支持注释。建议停止程序后编辑文件，再启动生效；运行中使用 WebUI 保存只写入统一文件，点击设置页顶部“保存并重启”后统一生效。

## 首次启动与配置校验

程序只读取应用目录中的 `tdl_config.json`。文件不存在时生成当前版本的完整默认配置；文件损坏、版本不支持或含有未知字段时直接报错。不导入 `config.json`、`components/` 或旧存储驱动，也不提供迁移命令。可以执行 `tdl config-init --home <应用目录>` 离线创建或校验配置，无需启动 Telegram、aria2 或 WebUI。

敏感值与其他配置一起保存在统一文件，写入权限为 0600（Windows 使用文件系统 ACL）；WebUI 查询不回显密钥。根目录实际 `tdl_config.json` 已加入 Git 和 Docker 构建忽略规则。分享配置时使用默认模板。

运行时快照仅由组件 schema 和统一仓库生成，缺少仓库时直接报错；不存在平面配置覆盖或第二套宽松校验。嵌套规则对象的未知字段也会被拒绝。历史兼容清理范围和开发约定见 [开发基准](baseline.md)。

## 配置范围与启动参数

`TDL_HOME` 环境变量指定应用目录，未设置时使用可执行文件所在目录；它在读取配置前确定，不能放进同一个 JSON。统一文件固定为 `<应用目录>/tdl_config.json`，会话与任务数据库位于 `.tdl/`，日志位于 `.tdl/log/`。`config-init --home` 仅选择离线工具操作的目录；普通启动仍使用 `TDL_HOME`。没有其他业务配置环境变量覆盖。数据库记录、登录会话、任务状态、浏览器筛选条件与外部 aria2/Docker 的配置不属于 TDL 设置。日志容量/轮转、HTTP 内部缓冲等代码常量当前不是用户配置项。所有现有用户设置及可调重试/超时/清理周期都列在下面。

未启动或已停用组件仍可编辑配置。所有设置经校验后仅保存到文件，不自动重启或修改运行中的服务。启动配置保持不变，断线重连也继续使用启动值。WebUI 中秘密字段省略或留空表示保留已保存的值；直接编辑 JSON 时，空值表示清空，省略则使用默认值。NTP 自动填写沿用 master：每次正常启动时，优先检测已配置服务器，最多尝试 3 次、每次超时 3 秒；留空或不可用时，并发检测内置服务器，选择响应最快的可用地址。结果写入 `components["account.telegram"].values.ntp` 后再创建运行快照；全部不可用则清空该字段并使用系统时间。可用的自定义服务器优先保留。内置候选为 cn.pool.ntp.org、ntp.aliyun.com、ntp.tencent.com、ntp.sjtu.edu.cn、ntp.nju.edu.cn、time1.google.com、time1.apple.com、time.cloudflare.com、time.windows.com。启动选择结果记录在日志中。离线 config-init、文档生成、WebUI 刷新和保存均不探测；WebUI 修改在手动重启后参与下一次选择。

网络代理统一在 WebUI 的“网络连接 → 网络代理与连接”中配置；网络连接位于设置标签首位并默认打开，选择协议后填写 IP 或域名加端口，需要认证时展开填写用户名和密码。由 account.telegram.proxy 保存，Telegram、机器人和软件更新共用。模块启停位于独立“模块管理”；运行日志、组件健康及诊断事件位于“检查更新”之前的独立“日志管理”，支持按全部功能、功能分组和细分模块查看。软件升级位于独立“检查更新”。

WebUI 设置覆盖全部 75 个组件字段及两个系统设置。全部字段直接展开，支持按中文名称、说明或字段名搜索并定位；搜索不索引实际配置值。分类和区块显示未保存数量。顶部仅保留“刷新”，从 tdl_config.json 重新读取配置，替换当前未保存输入。下载方式通过勾选和排序配置，http 始终位于最后；本地下载目录与 aria2 保存目录分别说明所属机器。字段显示默认值、范围和生效方式，输入错误在字段旁提示。账号数据空间只展示，切换入口在账号管理；完全重置位于系统的应用维护区。

有修改时，顶部显示“尚未保存的修改”框，逐项对比当前运行值和修改后的值，并区分尚未保存与已保存待重启；没有修改时隐藏整个框及按钮。框内“保存并重启”一次校验并保存所有分类的草稿，然后请求重启；也可用区块按钮先保存文件。校验或保存失败不重启，未保存输入保留，已成功写入文件的区块会单独标记。普通字段及允许操作机器人的用户列表直接回显，代理和 aria2 地址显示去掉认证信息的预览；API Hash、Token、密码和 RPC 密钥不回显。刷新页面仍可看到文件与运行值的差异，改回启动值并保存后移除对应项。重启会暂时中断连接和任务，新配置生效后清单清空；如修改了面板地址或凭据，请使用新地址或凭据重新登录。

`download.control.executors` 可设置为 `["aria2","local","http"]`，选择本地执行器时必须同时提供本机绝对路径 `local_root`。只有明确未接受任务的错误允许降级；超时、响应丢失或已有任务 ID 时停止提交。aria2 仍需启用对应模块和自动下载。列表不能为空，默认顺序为 aria2、http；保存并重启后按新顺序提交任务。只接受 local、aria2、http，不接受 internal 或 mode 字段；本地任务只使用 download.local 键空间，任务记录必须具有当前状态与版本字段，不自动补齐历史记录。

所有配置字段及模块启停均需用户手动重启进程后生效，包括下载执行器、过滤/命名、转发规则和详细日志。配置保存不触发服务协调或自动重启。账号切换、程序升级与完全重置继续由各自明确的操作入口执行。

HTTP 下载记录必须包含有效的 `last_active_at`；缺失或损坏时读取、续期和清理会报错，不回退到创建时间或索引时间，也不自动补写。软件更新只接受与当前操作系统、架构和 ARM 版本完全匹配的发布包，以及包内根目录的 `tdl` / `tdl.exe`。

WebUI 的“系统 → 应用维护 → 完全重置”须再次确认后执行。重置会停止服务、关闭数据库与日志，再清空应用目录内 `.tdl/` 的内容，删除 `tdl_config.json` 及其写入临时文件，包含全局配置、所有账号的登录凭据和任务记录。数据目录本身保留以支持挂载；完成后程序退出，下次启动需重新配置与登录，容器遵循自身重启策略。清理结果显示在程序控制台。

新配置的 Telegram 内置预设默认为 `desktop`，WebUI 提供下拉选择并提示不建议修改；“使用内置 API 凭据”默认勾选，位于账号设置的常用选项。API ID 未设置时显示空输入框，API Hash 不回显，两者留空均保留原值。内置预设必须明确选择；已有会话缺少凭据指纹时需要重新登录，不再猜测原应用。断线重试和时间校准直接显示在“网络连接”中；重连间隔必须为 1–86400 秒，默认 3 秒，0 不再作为另一套默认值的别名。

日志管理默认显示当前账号和系统公共记录，支持级别、诊断类型、时间、关键词筛选及 JSON 导出；每页 200 条，最新页每 5 秒刷新，历史页与离开页面时停止自动刷新。面板查询最近 2000 条结构化记录，写入 `.tdl/log/events.jsonl`；原有 `latest.log` 继续保留。两类文件每 10 MB 轮转，备份最多 3 份且最长保留 7 天。重启恢复当前结构化日志文件，轮转备份需在服务器文件系统查看；旧版本只有文本日志的记录不自动转换。详细日志开关位于系统设置，保存并手动重启后生效。CMD／终端仅保留启动、停止和 WebUI 地址等简短提示，详细运行日志在 WebUI 查看；文本日志和结构化日志统一脱敏，日志写入故障按输出端在页面提示，已有内存记录仍可查看。沿用 DEBUG（高频细节与正常取消）、INFO（关键结果）、WARN（可恢复异常）、ERROR（最终失败）四级；默认 INFO，详细日志开启后记录 DEBUG。分级、字段和开发约定见 [日志规范](logging.md)。

## account.telegram — Telegram 账号

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `proxy` | 统一网络代理 | `string` | `""` | proxy | 是 | 是 | network / 网络代理与连接 | Telegram、机器人和软件更新共用此代理。选择协议后填写 IP 或域名加端口，例如 127.0.0.1:1080；需要认证时展开填写。整体留空保留已保存的代理，修改时请同时填写所需的认证信息。 |
| `ntp` | 时间校准服务器 | `string` | `""` | — | 否 | 是 | network / 网络代理与连接 | 启动时优先检测已填写的服务器，最多尝试 3 次。留空或检测失败时，从内置服务器中自动选择最快的可用地址并写入配置；全部不可用则清空此项，使用系统时间。修改后保存并重启生效。 |
| `file_limit` | 同时下载的文件数 | `int` | `1` | ≥ 1，≤ 10000 | 否 | 是 | download / 下载并发与节奏 | 限制同时下载的 Telegram 文件数。调大前请确认网络带宽和账号连接稳定。 |
| `dc_pool_size` | 每个数据中心的连接数 | `int` | `8` | ≥ 1，≤ 10000 | 否 | 是 | download / 下载并发与节奏 | 每个 Telegram 数据中心可用于下载的连接容量，通常保留默认值。 |
| `delay_seconds` | 任务间隔（秒） | `int` | `0` | ≥ 0，≤ 3600 | 否 | 是 | download / 下载并发与节奏 | 任务之间的等待时间；0 表示不额外等待。 |
| `reconnect_timeout_seconds` | 断线重试间隔（秒） | `int` | `3` | ≥ 1，≤ 86400 | 否 | 是 | network / 网络代理与连接 | Telegram 监听连接中断后，等待这些秒再重新连接。 |
| `use_builtin` | 使用内置 API 凭据 | `bool` | `true` | — | 否 | 是 | account / Telegram API 凭据 | 通常保持开启，无需申请 API 凭据。使用自己的 API ID 和 API Hash 时关闭此项，并成对填写下方凭据。 |
| `api_id` | 自定义 API ID | `int` | `0` | ≥ 0 | 否 | 是 | account / Telegram API 凭据 | 从 my.telegram.org 获取，与 API Hash 配套使用。仅在关闭「使用内置 API 凭据」后优先使用；留空保留原值。 |
| `api_hash` | API Hash | `string` | `""` | — | 是 | 是 | account / Telegram API 凭据 | 留空保持不变。填写自定义 API ID 时需同时提供对应的 API Hash。 |
| `builtin_preset` | 内置预设 | `string` | `"desktop"` | desktop / builtin | 否 | 是 | account / Telegram API 凭据 | 默认 desktop，不建议修改。更换预设可能需要重新登录。 |

## configuration.manager — 统一配置管理

运行作用域：`process`。

固定启用，统一管理顶层 system 设置及全局组件设置；自身不另建组件配置。

## console.bot — Bot 控制台

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `token` | 机器人 Token | `string` | `""` | — | 是 | 是 | bot / 机器人访问 | 填写从 Telegram @BotFather 获取的 Token，用于机器人命令及通知。留空保留原值。 |
| `allowed_users` | 允许操作机器人的用户 | `strings` | `[]` | — | 否 | 是 | bot / 机器人访问 | 每行一个 Telegram 数字用户 ID，不能填写 @用户名。留空时不允许任何用户执行机器人命令。 |

## download.control — 下载任务控制

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `executors` | 下载方式与尝试顺序 | `strings` | `["aria2","http"]` | — | 否 | 是 | download / 下载方式 | 勾选要使用的下载方式，按标出的顺序尝试。仅在上一种方式明确未接收任务时尝试下一种；仅生成链接始终放在最后。 |
| `local_root` | 本地保存目录 | `string` | `""` | — | 否 | 是 | download / 下载方式 | 选择 local 时必填。填写 TDL 所在机器上的绝对路径，例如 D:\Downloads 或 /data/downloads；容器部署时填写容器内路径。 |

## downloader.aria2 — aria2 下载器

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `auto_download` | 自动提交到 aria2 | `bool` | `true` | — | 否 | 是 | download / aria2 连接 | 使用 aria2 下载方式时需要开启；关闭后不再自动向 aria2 提交新任务。 |
| `rpc_url` | aria2 RPC 地址 | `string` | `"http://127.0.0.1:6800/jsonrpc"` | url | 是 | 是 | download / aria2 连接 | 显示配置文件中的地址，认证信息不回显。填写 TDL 能访问的完整 RPC 地址，例如 http://127.0.0.1:6800/jsonrpc；留空保留原值。容器中的 127.0.0.1 指容器自身。 |
| `secret` | aria2 RPC 密钥 | `string` | `""` | — | 是 | 是 | download / aria2 连接 | 与 aria2 的 rpc-secret 一致。留空保留原值。 |
| `directory` | aria2 保存目录 | `string` | `""` | — | 否 | 是 | download / aria2 连接 | 填写 aria2 所在机器上的目录；留空使用 aria2 自身的默认下载目录。 |
| `timeout_seconds` | 连接超时（秒） | `int` | `30` | ≥ 1，≤ 3600 | 否 | 是 | download / aria2 连接 | 单次 aria2 RPC 请求的最长等待时间。 |
| `status_interval_ms` | 状态同步间隔（毫秒） | `int` | `60000` | ≥ 100，≤ 3600000 | 否 | 是 | download / aria2 连接 | 向 aria2 查询任务状态的间隔；1000 毫秒 = 1 秒。 |
| `connect_retry_ms` | 连接首次重试间隔（毫秒） | `int` | `10000` | ≥ 100，≤ 3600000 | 否 | 是 | download / aria2 连接 | 连接失败后从此间隔开始重试，不能大于最大重试间隔。 |
| `connect_retry_max_ms` | 连接最大重试间隔（毫秒） | `int` | `60000` | ≥ 100，≤ 3600000 | 否 | 是 | download / aria2 连接 | 连续连接失败时，重试等待时间增长的上限。 |
| `monitor_poll_ms` | 无速度检查间隔（毫秒） | `int` | `30000` | ≥ 100，≤ 3600000 | 否 | 是 | download / aria2 连接 | 检查下载中任务是否长时间没有速度的频率。 |
| `monitor_stall_seconds` | 无速度持续时间（秒） | `int` | `180` | ≥ 1，≤ 86400 | 否 | 是 | download / aria2 连接 | 任务持续没有速度达到此时间后，尝试暂停再恢复。 |
| `monitor_pause_seconds` | 无速度恢复等待（秒） | `int` | `10` | ≥ 1，≤ 3600 | 否 | 是 | download / aria2 连接 | 恢复无速度任务时，暂停后等待多久再继续下载。 |
| `monitor_action_seconds` | 无速度恢复操作超时（秒） | `int` | `30` | ≥ 1，≤ 300 | 否 | 是 | download / aria2 连接 | 执行暂停或恢复操作时的最长等待时间。 |
| `error_window_seconds` | Telegram 错误统计时段（秒） | `int` | `10` | ≥ 1，≤ 3600 | 否 | 是 | download / aria2 连接 | 在最近这段时间内累计 Telegram 下载错误次数。 |
| `error_threshold` | 触发恢复的错误次数 | `int` | `3` | ≥ 1，≤ 10000 | 否 | 是 | download / aria2 连接 | 统计时段内错误达到此次数后，尝试暂停再恢复下载。 |
| `error_cooldown_seconds` | 错误恢复冷却时间（秒） | `int` | `10` | ≥ 1，≤ 3600 | 否 | 是 | download / aria2 连接 | 两次错误恢复操作之间的最短间隔，避免频繁操作。 |
| `error_pause_seconds` | 错误恢复等待（秒） | `int` | `5` | ≥ 1，≤ 3600 | 否 | 是 | download / aria2 连接 | 因 Telegram 错误暂停下载后，等待多久再恢复。 |
| `error_action_seconds` | 错误恢复操作超时（秒） | `int` | `30` | ≥ 1，≤ 300 | 否 | 是 | download / aria2 连接 | 因 Telegram 错误执行暂停或恢复操作时的最长等待时间。 |

## downloader.local — 本地下载器

运行作用域：`connection`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `poll_interval_ms` | 任务检查间隔（毫秒） | `int` | `5000` | ≥ 100，≤ 3600000 | 否 | 是 | download / 本地下载器 | 本地下载器检查待执行任务的间隔，仅使用 local 下载方式时生效。 |
| `shutdown_timeout_seconds` | 停止时保存进度超时（秒） | `int` | `5` | ≥ 1，≤ 300 | 否 | 是 | download / 本地下载器 | 停止本地下载器时，等待任务暂停并保存记录的最长时间。 |

## filter.rules — 下载过滤规则

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `include` | 允许的扩展名 | `strings` | `[]` | — | 否 | 是 | download / 文件过滤 | 例如 mp4、mkv，每行一项。与排除列表互斥。 |
| `exclude` | 排除的扩展名 | `strings` | `[]` | — | 否 | 是 | download / 文件过滤 | 例如 png、jpg，每行一项。与允许列表互斥。 |
| `min_mb` | 最小文件大小（MB） | `int` | `0` | ≥ 0 | 否 | 是 | download / 文件过滤 | 包含边界，0 表示不限制。 |
| `max_mb` | 最大文件大小（MB） | `int` | `0` | ≥ 0 | 否 | 是 | download / 文件过滤 | 包含边界，0 表示不限制。 |

## forward.rules — 分组转发规则

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `rules` | 来源 → 目标 | `objects` | `[]` | — | 否 | 是 | forward / 转发规则 | 按来源分组指定转发目标、转发方式和静默选项。添加或修改规则后，点击本区块的保存按钮生效。 |

`rules` 每项包含 `id`（非空且唯一）、`name`（最多 100 字）、`enabled`、`sources`（来源数组）、`targets`（目标数组）、`mode`（default/clone）、`silent`。来源/目标格式为 `user:123`、`chat:123`、`channel:123`；目标也可为 `self`。最多 200 条规则，每条最多 500 个来源和 100 个目标，不允许转发环路。示例：`{"id":"saved","name":"收藏","enabled":true,"sources":["channel:123"],"targets":["self"],"mode":"default","silent":false}`。

## forwarder — 转发队列

运行作用域：`connection`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mode` | 默认转发方式 | `string` | `"default"` | default / clone | 否 | 是 | forward / 默认转发与重试 | 转发保留消息来源，复制以当前账号重新发送。匹配规则时使用规则指定的方式。 |
| `target` | 默认目标 | `string` | `""` | — | 否 | 是 | forward / 默认转发与重试 | 未指定目标时使用；留空表示收藏夹。已匹配规则的来源使用规则目标。 |
| `silent` | 静默发送 | `bool` | `false` | — | 否 | 是 | forward / 默认转发与重试 | 发送时不触发接收方的声音通知；匹配规则时使用规则指定的选项。 |
| `dedupe_ttl_seconds` | 重复任务忽略时长（秒） | `int` | `600` | ≥ 1，≤ 8640000 | 否 | 是 | forward / 默认转发与重试 | 在此时间内忽略重复提交的同一转发任务。 |
| `poll_interval_ms` | 任务检查间隔（毫秒） | `int` | `2000` | ≥ 100，≤ 3600000 | 否 | 是 | forward / 默认转发与重试 | 检查等待执行或等待重试的转发任务的间隔。 |
| `retry_base_seconds` | 首次重试间隔（秒） | `int` | `5` | ≥ 1，≤ 86400 | 否 | 是 | forward / 默认转发与重试 | 转发失败后从此间隔开始重试，不能大于最大重试间隔。 |
| `retry_max_seconds` | 最大重试间隔（秒） | `int` | `300` | ≥ 1，≤ 86400 | 否 | 是 | forward / 默认转发与重试 | 连续失败时，重试等待时间增长的上限。 |
| `max_attempts` | 失败次数上限 | `int` | `10` | ≥ 1，≤ 100 | 否 | 是 | forward / 默认转发与重试 | 累计失败达到此次数后停止自动重试。 |
| `history_hours` | 历史任务保留时间（小时） | `int` | `24` | ≥ 1，≤ 8760 | 否 | 是 | forward / 默认转发与重试 | 已结束任务超过此时间后清理，不删除 Telegram 消息。 |
| `history_limit` | 历史任务保留数量 | `int` | `200` | ≥ 1，≤ 100000 | 否 | 是 | forward / 默认转发与重试 | 最多保留这些已结束任务；超出时优先清理较早的记录。 |

## naming.rules — 目录与文件命名

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `filename` | 文件名模板 | `string` | `"P_S_F"` | — | 否 | 是 | download / 目录与文件命名 | 支持 G 名称、P 来源 ID、I 消息文字、F 原始文件名、S/R 消息 ID、A 相册 ID、Y/M/D 日期，例如 G-I-F。 |
| `directory` | 目录模板 | `string` | `"G\\Y\u0026M"` | — | 否 | 是 | download / 目录与文件命名 | 使用与文件名相同的变量；I 会保留中英文及数字并自动截断。 |
| `max_bytes` | 文件名长度上限（字节） | `int` | `255` | ≥ 0，≤ 255 | 否 | 是 | download / 目录与文件命名 | 按 UTF-8 字节计算，中文通常每字占 3 字节。0 使用 255 字节上限；过长的文件名会截短。 |

## notify.telegram — Telegram 通知

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `recipients` | 通知接收者 | `strings` | `[]` | — | 否 | 是 | notifications / 下载通知 | 每行一个 Telegram 用户或群组的数字 ID，群组 ID 可为负数。需先配置机器人 Token，并确保机器人能向接收者发送消息；留空不发送通知。 |
| `on_download_start` | 下载开始时通知 | `bool` | `false` | — | 否 | 是 | notifications / 下载通知 | 任务开始下载时发送消息。 |
| `on_download_complete` | 下载完成时通知 | `bool` | `false` | — | 否 | 是 | notifications / 下载通知 | 任务下载完成时发送消息。 |
| `on_download_pause` | 下载暂停时通知 | `bool` | `false` | — | 否 | 是 | notifications / 下载通知 | 任务暂停时发送消息。 |
| `on_download_error` | 下载失败时通知 | `bool` | `false` | — | 否 | 是 | notifications / 下载通知 | 任务下载出错时发送消息。 |
| `live_progress` | 持续更新下载进度 | `bool` | `false` | — | 否 | 是 | notifications / 下载通知 | 通过编辑进度消息显示下载状态；接收者列表不能为空。 |
| `live_progress_interval_seconds` | 进度更新间隔（秒） | `int` | `5` | ≥ 5，≤ 86400 | 否 | 是 | notifications / 下载通知 | 仅在开启持续更新下载进度时生效，增大间隔可减少消息更新频率。 |
| `timeout_seconds` | 发送超时（秒） | `int` | `15` | ≥ 1，≤ 300 | 否 | 是 | notifications / 下载通知 | 向每位接收者发送或更新通知时的最长等待时间。 |

## panel.webui — Web 管理面板

运行作用域：`process`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `username` | 登录用户名 | `string` | `"admin"` | nonempty | 否 | 是 | panel / 面板访问 | 用于登录此管理面板，不能为空。修改后请使用新用户名登录。 |
| `password` | 登录密码 | `string` | `"admin"` | — | 是 | 是 | panel / 面板访问 | 填写新密码以替换原密码；留空保留原值。修改后请使用新密码登录。 |
| `address` | 服务监听地址 | `string` | `"0.0.0.0"` | — | 否 | 是 | panel / 面板访问 | 0.0.0.0 监听所有 IPv4 网卡；127.0.0.1 仅允许本机访问。修改后可能需要重新打开面板。 |
| `port` | 服务监听端口 | `int` | `22335` | ≥ 1，≤ 65535 | 否 | 是 | panel / 面板访问 | 修改后请通过新端口访问面板；容器部署时也需检查端口映射。 |

## proxy.range — HTTP Range 代理

运行作用域：`process`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `public_base_url` | 下载链接的访问地址 | `string` | `""` | url | 否 | 是 | links / 下载链接服务 | 生成下载链接前需填写下载者和 aria2 能访问的完整地址，例如 https://files.example.com 或 http://192.168.1.10:22334。 |
| `link_ttl_hours` | 链接闲置有效期（小时） | `int` | `24` | ≥ 0，≤ 876000 | 否 | 是 | links / 下载链接服务 | 从最近一次活动开始计算，超过后清理链接；0 表示不自动过期。 |
| `address` | 服务监听地址 | `string` | `"0.0.0.0"` | — | 否 | 是 | links / 下载链接服务 | 0.0.0.0 监听所有 IPv4 网卡；127.0.0.1 仅允许本机访问。这不是生成下载链接时使用的域名。 |
| `port` | 服务监听端口 | `int` | `22334` | ≥ 1，≤ 65535 | 否 | 是 | links / 下载链接服务 | 下载链接服务的本机端口。使用容器或反向代理时，访问地址需与实际端口映射对应。 |
| `client_wait_seconds` | 等待账号连接超时（秒） | `int` | `30` | ≥ 1，≤ 600 | 否 | 是 | links / 下载链接服务 | 下载请求到达后，等待 Telegram 账号连接就绪的最长时间。 |
| `persist_timeout_seconds` | 传输记录保存超时（秒） | `int` | `5` | ≥ 1，≤ 300 | 否 | 是 | links / 下载链接服务 | 将下载活动写入任务记录的最长等待时间。 |
| `task_cleanup_seconds` | 过期链接清理间隔（秒） | `int` | `3600` | ≥ 1，≤ 86400 | 否 | 是 | links / 下载链接服务 | 检查并清理已超过闲置有效期的链接记录的间隔。 |
| `source_cleanup_seconds` | 闲置连接清理间隔（秒） | `int` | `60` | ≥ 1，≤ 3600 | 否 | 是 | links / 下载链接服务 | 检查并释放不再使用的下载来源连接的间隔。 |

## storage.maintenance — 存储维护

运行作用域：`account`。

无配置字段。

## trigger.download — 下载触发意图

运行作用域：`connection`。

无配置字段。

## trigger.forward — 转发触发意图

运行作用域：`connection`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `listen` | 自动转发的监听来源 | `strings` | `[]` | — | 否 | 是 | forward / 自动转发的监听范围 | 每行一个来源，例如 channel:123、chat:123 或 user:123。留空只监听已启用转发规则中的来源。 |
| `listen_comments` | 同时监听频道评论 | `bool` | `true` | — | 否 | 是 | forward / 自动转发的监听范围 | 将监听频道关联讨论组中的评论也纳入转发监听。 |

## trigger.messagelink — 消息链接触发

运行作用域：`account`。

无配置字段。

## trigger.reaction — 表情触发

运行作用域：`account`。

| 字段 | 含义 | 类型 | 默认值 | 范围 / 格式 | 敏感 | 需要重启 | WebUI 分类 / 区块 | 填写说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `download` | 下载表情 | `strings` | `[]` | — | 否 | 是 | download / 表情触发 | 每行一个表情；留空允许任意表情触发下载。 |
| `forward` | 转发表情 | `strings` | `[]` | — | 否 | 是 | forward / 表情触发 | 每行一个表情；留空关闭表情触发转发，监听转发仍按规则工作。 |

## update.self — 版本更新

运行作用域：`account`。

无配置字段。

