# TDL WebUI 设计规范

本规范对应当前实现，适用于 TDL 的总览、下载管理、转发管理、账号管理、模块管理、设置、检查更新与登录页面。

## 1. 参考分析与设计方向

视觉参考：[Zenith Dashboard · Charts](https://zenith-shadcn.dashboardpack.com/charts)。2026-09-19 检查实际页面、计算样式和 CSS 变量，并核对 1440px、1920px、2560px、3840px 桌面视口的内容宽度，采用其默认浅色主题。

参考站以白色内容区、浅灰侧栏、近黑文字和按钮构成中性底色；橙、青绿、深蓝和琥珀色主要用于数据区分。24px 页面标题、16px 卡片标题与较小的灰色说明形成层级。260px 侧栏、64px 顶栏、24px 页面留白、细边框和 14px 卡片圆角构成整体秩序；图表采用等宽双列、非等宽双列及通栏的组合。

TDL 沿用这些规则，保留中文业务名称、动态组件导航和真实指标。顶部栏显示当前位置、当前 Telegram 账号名称和用户名；左下角显示 TDL 连接状态与版本，不再显示数据空间与监听状态；总览优先展示四项任务摘要、通栏速度曲线与两张等宽资源卡片，下载调度及诊断数据放入折叠区。参考站的浅阴影略作减弱，中文使用系统字体回退，状态提示采用独立语义色。页面不引入额外 UI 框架。

## 2. 配色

颜色统一定义在 [base.css](application/panel.webui/assets/static/css/base.css) 的 `:root` 中。以下十六进制值为本项目实际使用值；参考站的 OKLCH 色值已整理为 sRGB 配色。

### 基础色

| 用途 | CSS 变量 | 色值 | 使用规则 |
| --- | --- | --- | --- |
| 主色 | `--primary` | `#171717` | 主按钮、品牌标记、选中控件 |
| 深色文字强调 | `--primary-dark` | `#262626` | 标签、选中项文字 |
| 主按钮悬停 | `--primary-light` | `#404040` | 主按钮 hover |
| 主色浅底 / 分段控件底色 | `--primary-soft` / `--bg-strong` | `#F3F3F3` | 标签底、轨道、标签页容器 |
| 页面背景 / 卡片 | `--bg` / `--panel` | `#FFFFFF` | 主内容区、卡片、输入框 |
| 辅助背景 / 侧栏 | `--panel-soft` / `--sidebar` | `#FAFAFA` | 侧栏、表头、悬停行、登录背景 |
| 导航选中背景 | `--sidebar-active` | `#F0F0F0` | 当前页面导航项 |
| 正文与标题 | `--text` | `#0A0A0A` | 主体文字 |
| 次级文字 | `--muted` | `#737373` | 描述、时间、图例、字段帮助 |
| 导航文字 | `--sidebar-text` | `#404040` | 未选中导航项 |
| 普通边框 | `--line` | `#E5E5E5` | 卡片、表格分隔线、输入框 |
| 强边框 | `--line-strong` | `#D4D4D4` | 输入框 hover、浮层边框 |
| 深色按钮文字 | — | `#FAFAFA` | 主按钮上的文字 |

### 图表辅助色

| 变量 | 色值 | 当前映射 |
| --- | --- | --- |
| `--chart-1` | `#F54900` | CPU、内存总用量、gotd 下载速度 |
| `--chart-2` | `#009689` | 软件内存、活跃分片、下载进度 |
| `--chart-3` | `#104E64` | 保留堆、aria2 下载速度和活动任务 |
| `--chart-4` | `#FFB900` | 等待任务、累计错误统计条 |
| `--chart-5` | `#FE9A00` | HTTP 备用连接指示点 |

图表线条、提示框中的标记和图例必须引用相同变量。高饱和色用于数据，不用于大面积页面背景或普通正文；浅黄色数据条始终配合深色数字与文字标签。

### 状态色

| 语义 | 文字 / 主色 | 背景 | 边框 |
| --- | --- | --- | --- |
| 成功、运行中 | `#0F766E` | `#F0FDFA` | `#99F6E4` |
| 错误、危险操作 | `#DC2626` | `#FEF2F2` | `#FECACA` |
| 警告、待处理 | `#92400E` | `#FFFBEB` | `#FDE68A` |
| 信息、启用状态 | `#075985` | `#F0F9FF` | `#BAE6FD` |

分别使用 `--success`、`--danger`、`--warning`、`--info` 及对应的 `-soft`、`-line` 变量。状态色与黑色品牌主色分开维护。状态须同时有文字，不单独依靠颜色。默认凭据提醒使用警告色，保留显著提示和修改入口。

## 3. 字体与层级

默认字体栈：`Geist, PingFang SC, Microsoft YaHei, Segoe UI, sans-serif`。拉丁字符与数字使用本地打包的 Geist 可变字体，中文按操作系统回退；`font-display: swap` 保证文字及时显示，不依赖外部字体 CDN。

等宽内容使用 `SFMono-Regular, Consolas, Liberation Mono, monospace`，用于路径、标识符、日志和技术数值。

| 层级 / 场景 | 字号 | 字重 | 行高 |
| --- | --- | --- | --- |
| 页面标题 H1 | 24px | 700 | 32px |
| 卡片标题 H2 / H3 | 16px | 600 | 24px |
| 仪表盘主指标 | 30px | 600 | 36px |
| 手机仪表盘指标 | 26px | 600 | 32px |
| 模块汇总数字 | 32px | 600 | 32px |
| 转发汇总数字 | 30px | 600 | 33px |
| 正文 | 14px | 400 | 21px；段落 22px |
| 导航 | 14px | 400；选中 500 | 21px |
| 常规按钮 | 14px | 500 | 20px |
| 表格单元格 | 13px | 400 | 20px |
| 输入标签 / 顶部路径 | 13px | 500 | 19.5px |
| 卡片描述、图例、状态标签 | 12px | 400–500 | 18px |
| 表头 | 12px | 500 | 18px |
| 图表坐标 | 10.5px | 400 | SVG 定位 |

H1 字距 `-0.025em`，卡片标题 `-0.015em`，大指标 `-0.03em`。中文正文不增加装饰性字距；只在品牌英文副标记和导航分组中使用轻微字距。仪表盘指标使用 `tabular-nums`，减少刷新时的数字跳动。

## 4. 间距与留白

基础节奏为 4px；常用尺寸通过 `--space-*` 变量提供，业务布局使用相同数值。

| 尺度 | 变量 | 常见用途 |
| --- | --- | --- |
| 4px | `--space-1` | 导航项间距、分段标签容器内边距 |
| 8px | `--space-2` | 按钮组、标签到输入框、图标到文字 |
| 12px | `--space-3` | 导航图标间距、品牌图标间距 |
| 16px | `--space-4` | 卡片横向间距、信息网格、手机页面左右留白 |
| 20px | `--space-5` | 小型信息卡片、侧栏底部内边距 |
| 24px | `--space-6` | 桌面页面边距、卡片内边距、页面标题下方、模块之间 |
| 32px | `--space-8` | 登录表单顶部、独立配置页顶部、手机页面底部 |
| 48px | `--space-12` | 桌面页面底部、空状态垂直内边距 |

图表卡片列间距 16px、行间距 24px；卡片内部主要块之间 24px。表格单元格内边距 `14px 16px`，表单行 `20px 24px`。紧凑按钮、文字微间距和进度条可用 6px 等光学校正值。

## 5. 组件规则

### 按钮与输入框

- 常规按钮高至少 36px，内边距 `7px 14px`，圆角 8px，1px 边框；紧凑按钮高至少 30px，字号 12px，内边距 `4px 10px`。
- 主按钮为近黑底浅色文字，hover 为 `#404040`；次按钮为白底灰边，hover 为 `#F3F3F3`。危险按钮为浅红底红字，hover 为 `#FEE2E2`、边框 `#FCA5A5`。
- 禁用按钮透明度 0.45，移除阴影并显示不可操作光标。登录提交按钮高度至少 40px。
- 输入框高至少 38px，内边距 `8px 12px`，白底、8px 圆角、1px `--line` 边框；hover 使用 `--line-strong`。多行组件配置输入框高度至少 80px。
- 键盘焦点：2px `#737373` 外轮廓、3px 偏移，辅以 `0 0 0 3px rgb(115 115 115 / 0.22)`。链接、按钮和表单均保留可见焦点。

### 卡片与表格

- 主卡片：白底、14px 圆角、1px `#E5E5E5` 边框；典型内边距 24px，小型信息卡片 20px。
- 卡片阴影：`0 1px 3px rgb(0 0 0 / 0.06), 0 1px 2px rgb(0 0 0 / 0.04)`。
- 按钮与小控件阴影：`0 1px 2px rgb(0 0 0 / 0.05)`。
- 普通卡片不添加装饰性色条、渐变或悬停位移；状态卡可用浅语义色背景。业务中的状态信息行可保留 3px 内嵌语义色边线。
- 表头使用浅灰背景、次级文字，行间 1px 细分隔线，hover 使用 `#FAFAFA`。宽表格在 `.table-shell` 内横向滚动。
- 空状态使用次级文字并居中，保留 `48px 24px` 留白。

### 导航与顶部栏

- 桌面侧栏固定宽 260px，浅灰背景，右边 1px 分隔线。品牌区与顶栏均至少 / 固定 64px 高。
- 导航项高至少 40px，内边距 `8px 12px`，圆角 8px；图标 18px，线宽约 1.6px，间距 12px。
- 激活项用浅灰填充与较深文字，同时标记 `aria-current="page"`。导航链接无下划线。
- 顶栏高 64px，左右 24px，底部细边框；白底透明度 0.94、12px 背景模糊。左侧为当前位置，右侧为当前 Telegram 账号；连接状态位于左下角。
- 组件导航由后端声明产生；未配置专属图标的新页面使用通用图标。

### 标签、标签页与图表

- 通用状态标签：6px 圆角，至少 24px 高，`2px 8px` 内边距，12px/500 文字。账号属性徽章与登录步骤保留适合其语义的胶囊 / 圆形。
- 页面内分段标签：浅灰容器圆角 10px、内边距 4px；标签圆角 7px，高至少 32px，选中项白底轻阴影。
- 图表绘图区高 220px，线宽 2px，圆头圆角；网格用 `--line`、1px、`3 4` 虚线。图例圆点 8px，文字 12px。
- 图表 tooltip 为近白底、灰边、7px 圆角；阴影 `0 4px 8px rgb(0 0 0 / 0.08)`。图例、曲线、tooltip 色点引用同一图表变量。
- 进度轨道高 6px，胶囊圆角，浅灰底；进度、数量和状态由业务数据驱动。

## 6. 布局与响应式

| 视口 | 规则 |
| --- | --- |
| ≥1200px | 固定侧栏；总览任务摘要四列，速度曲线通栏，CPU / 内存各占半宽 |
| 1101–1199px | 总览任务摘要与资源卡片等宽双列 |
| ≤1100px | 仪表盘单列；页面标题和操作区允许换行 |
| ≤900px | 侧栏变为抽屉，宽 `min(280px, 100vw - 64px)`；正文无左侧偏移，左右边距 16px；信息网格单列 |
| ≤560px | 顶部保留菜单、当前页和截短账号；连接状态位于导航抽屉底部；图表卡内边距 `20px 16px`，指标 26px；操作按钮允许换行 |

主内容区采用流式全宽布局，不设置最大宽度。桌面内容宽度为视口宽度减去 260px 侧栏及左右各 24px 内边距；数据区和凭据提示条保持同宽、左右对齐。与参考站一致，1440px / 1920px / 2560px / 3840px 视口对应 1132px / 1612px / 2252px / 3532px 内容宽度，宽屏下不会额外增加居中留白。

页面按“标题与说明 → 页面操作 / 标签页 → 指标或摘要 → 业务内容”组织。业务页面及设置卡片保持全宽；设置输入区最大 600px，说明文字使用剩余空间。登录卡片最大宽 420px，居中展示。

抽屉打开时锁定页面滚动，主内容不可聚焦；Tab 在导航内循环，Esc、遮罩或选择页面关闭抽屉，并恢复菜单按钮焦点。返回桌面宽度会清除遮罩和内容禁用状态。支持跳到主要内容；遵循 `prefers-reduced-motion`，关闭过渡与装饰动画。

## 7. 实现入口与维护

- [公共样式](application/panel.webui/assets/static/css/base.css)：设计变量、页面外壳、导航、按钮、表格、表单、标签、登录。
- [仪表盘样式](application/panel.webui/assets/static/css/dashboard.css) 与 [图表渲染](application/panel.webui/assets/static/js/dashboard.js)：卡片布局、图例及统一图表配色。
- [页面外壳](application/panel.webui/assets/index.html) 与 [移动导航](application/panel.webui/assets/static/js/shell.js)：顶部栏、路径、响应式抽屉。
- 各业务组件的 `assets/static/css/`：只维护该业务特有的布局与状态，优先复用公共变量。
- [Geist 字体及许可证](application/panel.webui/assets/static/fonts/OFL.txt)：字体来自 [vercel/geist-font](https://github.com/vercel/geist-font)，随静态资源本地内嵌，遵循 SIL OFL 1.1。

local 和 aria2 使用同一套原生下载列表。AriaNg 通过「高级下载控制」在新标签页打开，并使用当前站点的服务端 RPC 代理；其第三方内部界面保持原样。当前规范只定义浅色主题。

静态资源由 Go 内嵌，代码更新后需重新构建并启动服务，现有可执行文件不会自动读取新样式。


## 8. 页面结构与设置归属

一级导航由组件 Manifest 声明，顺序固定为总览、下载管理、转发管理、账号管理、模块管理、设置、检查更新。下载、转发、账号和检查更新入口在组件停用时保留并提供启用指引。模块管理聚合运行状态和服务开关；检查更新独立于设置。

| 页面 | 内容 |
| --- | --- |
| 总览 | 当前账号下载 / 转发任务与异常入口，速度、CPU、内存，折叠的调度诊断 |
| 下载管理 | 任务、链接记录两个二级标签；统一 local / aria2 的筛选与操作 |
| 转发管理 | 状态摘要、队列与批量操作，链接至转发设置 |
| 账号管理 | Telegram 账号信息、登录、会话切换与删除、Spam 检查 |
| 模块管理 | 按业务功能分组，组内显示细分模块状态、启停操作及日志入口 |
| 设置 | 六个业务标签，默认打开首个「网络连接」；每个配置区块独立保存、撤销 |
| 日志管理 | 运行日志、组件健康与诊断事件；支持全部功能、功能分组和细分模块筛选 |
| 检查更新 | 当前版本；正式发行版本与预览版各最近 5 个；选择版本查看说明并确认切换；保留 Docker 更新限制 |

| 设置标签 | 字段归属 |
| --- | --- |
| 网络连接 | 统一代理的协议、地址、认证；断线重试与时间校准 |
| 下载设置 | HTTP 下载服务、访问地址、闲置有效期、清理；下载方式、本机与 aria2 目录、命名、过滤、下载并发与间隔、下载表情 |
| 转发设置 | 转发规则、监听范围、默认转发、重试与历史记录、转发表情 |
| 机器人设置 | Bot Token、允许用户 |
| 通知设置 | 接收人、通知事件、进度、间隔、超时 |
| 系统设置 | WebUI 地址、端口、用户名、密码；Telegram API 凭据、内置预设及登录与切换账号入口；日志、应用维护及未分类扩展配置 |

组件拥有字段的 `settings_tab`、`settings_section`、`settings_order`、`advanced`、`help` 展示元数据。每个字段只在一个分类编辑；未分类的扩展字段进入系统高级组件配置。无配置字段的组件只在服务列表出现。组件停用不会禁止配置编辑；`editable` 和 `can_toggle` 仍由服务端决定。

所有设置统一由 `configuration.manager` SWC 保存到应用目录的 `tdl_config.json`，包含系统日志开关、当前登录命名空间及全局组件设置。同一进程只运行一个 Telegram 账号；命名空间用于选择登录会话与历史任务，切换时沿用全局设置。WebUI 按功能编辑，保存只写文件；设置页顶部对比启动值与已保存值，用户确认后手动重启进程统一生效。运行组件与重连流程使用只读的启动配置快照。首次启动生成当前版本的默认配置；字段与手工编辑规则见 [统一配置参考](docs/configuration.md)。

一级路由为 `/dashboard`、`/downloads`、`/forwards`、`/user`、`/modules`、`/config`、`/logs`、`/update`。下载标签使用 `?tab=tasks|links`，设置标签使用 `?tab=network|download|forward|account|links|bot|notifications|panel|system`。旧 `/kv`、`/components.html` 分别跳转到链接记录、高级组件配置；`/modules` 与 `/update` 直接访问独立页面。旧 `/config?tab=system#services`、`#diagnostics`、`#updates` 分别跳转到模块管理、日志管理的组件健康标签与检查更新；旧 `/modules#diagnostics` 也跳转到 `/logs?tab=health`。旧 `/config?tab=system#setting-account.telegram-proxy` 转到网络配置。未指定标签或标签无效时默认打开网络配置；标签支持刷新及浏览器前进后退。

检查更新按正式发行版本、预览版分栏展示，窄屏上下排列。每组最多 5 个，按发布时间倒序，不足时显示实际数量；草稿不展示。点击版本只切换选中状态及对应的 Release Notes、发布日期、安装包和发布页链接。通过「下载并切换」二次确认所选版本并重启，支持升级、回退与正式版／预览版互换。当前版本、无适配安装包的版本以及 Docker 环境仍能查看说明，但禁用安装并展示原因。下载期间锁定选择与操作，失败后允许重试；检查失败时清除过期的可安装目标。

Release Notes 使用服务端安全渲染的 Markdown，支持标题、强调、列表、引用、代码块、表格、任务列表及图片；原始 HTML 和危险链接不进入页面。说明链接在新标签页打开，代码块与宽表格在区域内滚动，切换版本后重置说明滚动位置。

## 9. 任务、表单与抽屉交互

- 任务区顺序为筛选工具栏、连接提示、选择后的批量操作栏、表格。主要列保留文件名、执行器、状态、进度、速度、创建时间及操作；路径、标识符和错误移入详情。
- 任务主键为账号、执行器和任务 ID 的组合。跨执行器批量操作分别提交，反馈成功、跳过与错误；离线执行器保留最后数据并标记过期，停用其操作。缺失值显示「—」。aria2 已终止的错误结果不能直接恢复，提示从链接记录重新提交。
- 原生列表只展示当前账号由 TDL 管理的任务。aria2 的连接凭据留在服务端；高级控制入口不构成独立权限边界。
- 配置区块使用 14px 圆角、1px 边框、24px 内边距，内容宽度最多 1120px；字段纵向内边距 16px。桌面采用标签 / 输入两列，说明、默认值、范围及生效方式统一放在输入下方，600px 以下全部单列。
- 分类标签依次为「网络连接、下载设置、转发设置、机器人设置、通知设置、系统设置」，窄屏自动换行。Telegram API 凭据并入系统设置；下载链接服务放在下载设置最前面，面板访问放在系统设置最前面；系统设置不再展示当前账号数据空间。顶部搜索覆盖全部字段的中文名称、说明和配置键，不搜索配置值；结果显示所属分类和区块，点击后聚焦字段，所有设置直接展开。
- 下载方式通过勾选「交给 aria2 下载、保存到本机、仅生成下载链接」及上下移动排序，链接方式固定最后。至少选择一种；选择本机下载时要求填写本地保存目录。目录说明明确区分 TDL 与 aria2 所在机器。
- 「保存此区块」只提交该区块的修改；分类徽标和区块底部显示未保存数量。修改字段展示已保存值，敏感字段不回显。数值留空不自动转为 0；范围、必填、文件过滤互斥、大小范围和重试间隔等错误在字段旁显示，提交时定位出错字段。
- 表单只提交修改字段，同一组件的各区块共享版本号。切换标签保留草稿；顶部“刷新”从配置文件重新读取并替换当前草稿；409 冲突不自动覆盖。密钥留空保留原值，草稿只保存在内存。
- 离开设置、退出登录或浏览器刷新时提示未保存修改；确认离开后撤销草稿。配置通过组件 API 保存，系统 API 仅提供命名空间展示和详细日志开关。
- 转发规则列表直接展示配置：标题行放规则名称及启停开关，中间按「来源 → 目标」展示全部已选聊天名称及用户名，未读取到名称时保留聊天 ID，`self` 显示为「收藏夹」。底部明确展示转发模式和静默发送的开启／关闭状态，编辑、排序、复制与删除按钮与启停开关分区放置。卡片沿用浅灰背景、1px 边框及 24px 内边距，聊天标签使用 6px 圆角并自动换行；≤600px 来源与目标纵向排列，内边距 16px。抽屉内编辑完成后列表立即反映草稿，「保存更改」统一提交整个规则区。
- 详情和规则抽屉桌面宽 `min(640px, 50vw)`，900px 以下占满视口；头部 20px / 24px 留白，正文 24px。打开时锁定背景、焦点进入抽屉、Tab 循环，Esc 关闭并恢复触发控件焦点。
- 页面共用一个 WebSocket。使用 `download-tasks-local`、`download-tasks-aria2` 观察主题；HTTP 仅承担命令、快照和断线轮询回退。页面切换取消不再使用的订阅。

新增布局与交互样式位于 [workspace.css](application/panel.webui/assets/static/css/workspace.css)，复用基础设计变量。通用抽屉与标签键盘操作由 [ui.js](application/panel.webui/assets/static/js/ui.js) 提供。

### 当前账号、功能模块与统一代理

- 右上角账号入口使用 34px 圆形首字头像、13px/500 账号名称、11px 用户名。点击进入账号管理；窄屏截断长名称，320px 下隐藏次要文字。账号信息取自 `/api/user`，不以命名空间代替 Telegram 身份，不显示电话号码；后台查询失败显示不可用状态。身份仅保存在内存，60 秒内共用缓存，账号页刷新会同步顶栏。
- 左下角使用「TDL 状态」标题，状态文字统一为「已连接／断开连接」，并显示版本号；不显示数据空间与监听状态。WebSocket 或 HTTP 回退连接成功时，7px 绿色灯点以 2.8 秒周期轻微呼吸（透明度 0.6–1），外围为 3px 淡色光圈与 7px 柔光；断线时使用静止红灯，连接尚未建立时使用灰灯。悬停查看连接详情，系统启用减少动态效果时停止动画。
- 转发管理的「进行中」数量紧邻页面标题，垂直居中、间距 12px，窄屏可换行；右侧操作区仅放置「转发设置」「刷新」按钮。
- 模块按 Manifest 的 `feature` 元数据归入下载管理、转发管理、下载链接服务、账号管理、机器人与通知、表情触发、面板与数据维护、软件更新。扩展组件未声明分组时进入「扩展功能」。功能卡片展示模块总数、运行数和待关注数，展开后展示组件名称、标识、实际状态与独立启停按钮。业务分组不改变原有服务依赖或启停语义。
- 功能卡使用 14px 圆角、1px 边框和浅阴影，24px 模块间距。桌面采用两列独立纵向排列，各列卡片按内容高度紧接上一张卡片；展开、折叠或状态内容变化时，本列后续卡片自动跟随，不与另一列按行对齐。≤1200px 按原功能顺序排列为一列，切换布局时保留展开状态与键盘焦点。组内细分模块使用分隔行，不嵌套卡片。运行中、已停用、启动中、依赖未就绪、异常和等待重启分开显示。状态每 10 秒刷新，离开页面停止刷新；失败保留上次数据并提示。
- 代理只有「网络配置 → 网络代理」一个编辑位置，`account.telegram.proxy` 保存统一地址。Telegram、Bot、软件检查更新与下载更新共用该地址；更新组件通过 `network.proxy` 端口获取当前值。API 拒绝服务专属代理字段；空密钥保留原值。
- 网络连接沿用设置卡片及两列表单布局，协议下拉框与地址输入框间距 8px、控件高度 38px、圆角 8px，≤600px 上下排列。协议支持 SOCKS5、SOCKS5H、HTTP、HTTPS；地址填写 `IP:端口` 或 `域名:端口`，IPv6 使用 `[地址]:端口`。可选代理认证默认折叠，用户名和密码间距 12px，≤600px 单列；密码始终遮蔽。粘贴完整代理 URL 时拆分协议、地址和认证信息，保存时编码并合成为原有单一代理字段，保留显式端口。代理草稿仅在内存中保留，保存或撤销后清空输入，整体留空不修改既有代理。
- 转发规则的「静默发送」为单行可点击标签，复选框位于文字左侧，16px 大小、8px 间距，垂直居中；保持与其他规则字段一致的行间距。

### 完全重置

- 系统设置不再显示空的「高级组件配置」标题；扩展字段按其声明的设置位置显示。
- 系统页先展示当前账号数据空间及账号管理入口，再展示日志开关；「完全重置…」位于应用维护卡片。“保存并重启”位于设置顶部的“尚未保存的修改”框，随修改框隐藏；一次校验并保存所有分类的输入，保存失败时不重启。重置点击后打开居中警告弹窗，宽度 `min(560px, 100vw - 32px)`、14px 圆角、24px 内边距，默认聚焦「取消」，Esc 可取消；确认提交期间禁用重复操作。
- 弹窗明确说明不可撤销、影响所有账号，并从服务端展示实际清理路径：应用目录内 `.tdl` 的全部内容、`tdl_config.json` 及其写入临时文件。
- 服务端仅接受已登录用户的 JSON 确认请求，不接受客户端指定删除路径。停止服务、关闭数据库与日志成功后执行清理；保留目录本身以兼容 Docker 挂载。路径异常或服务退出失败时终止，清理结果输出到程序控制台。
- 接受重置后清空界面草稿并停止实时订阅，面板显示正在退出与清理，不提前报告完成。完成后 TDL 退出；再次启动需重新配置并登录，容器按自身重启策略运行。

### 账号应用凭据

- 内置预设使用与现有表单一致的下拉框，新配置默认选 `desktop（推荐）`，旁边提示不建议修改；预设必须明确选择 `desktop` 或 `builtin`。
- 「使用内置 API 凭据」新配置默认勾选，显示在常用选项中；已保存的选择保持不变，取消勾选后才会优先使用自定义 API ID / API Hash。
- API ID 未设置时显示空输入框，API Hash 不回显；两者统一使用「留空保持不变」占位提示。清空输入不提交 0 或覆盖原值，保存自定义凭据仍需满足成对校验。Manifest 的 `empty_preserves` 声明留空保留行为，`choice_labels` 提供选项展示文字。
- 断线重试间隔直接显示在网络配置中，以「连接中断后等待多久再重试」解释。只调整展示位置和名称，保留原有重试语义。

- 使用默认 WebUI 管理员账号登录成功后，在顶部浮层提示修改用户名和密码，8 秒后自动消失，也可手动关闭或跳转到「系统设置 → 面板访问」。刷新页面、切换页面和运行状态更新不会重复提示；页面不再显示常驻警告。

### 日志管理

- 一级菜单「日志管理」放在「检查更新」前。页面使用「运行日志／组件健康」单层标签，沿用全宽布局、Geist 字体、黑白灰配色及 8px 控件圆角。模块管理每个细分模块提供对应日志快捷入口。
- 默认显示当前账号与系统公共记录，可按全部功能、功能分组、细分模块、级别、类型、时间和关键词组合查询。分组来自 Manifest，停用模块仍可筛选；无明确业务归属的记录进入「系统运行」。筛选和标签保存在 URL，刷新与前进后退保留选择。
- 日志表按新到旧排列，显示时间、级别、功能／细分模块、消息和详情入口；错误用红色、警告用琥珀色，其余使用中性色。表格容器独立滚动，表头固定，窄屏不撑宽页面；详情沿用右侧抽屉，文本可换行。
- 每页 200 条，支持查看更早日志与返回最新。最新页每 5 秒刷新，可暂停；历史页暂停自动刷新，离开页面中止请求和定时器。读取失败显示提示并保留上次结果；切换筛选时清空旧结果，避免误认模块归属。
- 导出遵循当前筛选，只导出页面可查询的保留记录；不绕过现有面板登录和账号范围。后台最多保留最近 2000 条结构化记录，写入 `.tdl/log/events.jsonl`；保留原有 `latest.log`。两个文件均按 10 MB 轮转，备份最多 3 份、最多 7 天；重启从当前结构化文件恢复时同样只保留最新 2000 条，轮转备份不在页面查询范围内。
- Zap、slog、组件生命周期及诊断错误统一接入；文本文件和结构化记录共用脱敏规则，不记录配置全文、登录输入或协议正文。详细日志沿用「系统设置」开关，保存后立即影响文件、WebUI 记录及已有子 logger。CMD／终端只保留原有的启动、停止和 WebUI 地址等简短提示，不打印结构化运行日志。日志写入失败时保留内存记录，并按输出端在页面提示。
- 沿用 DEBUG／INFO／WARN／ERROR 四级：高频请求、筛选和正常取消使用 DEBUG，关键结果和状态变化使用 INFO，可恢复异常使用 WARN，最终失败使用 ERROR。默认 INFO；连续重试保留首次 WARN，后续细节进入 DEBUG，恢复时记录 INFO。底层错误交由最终处理边界记录，避免重复告警。
- 日志保留真实调用位置、组件与账号归属；任务日志提供稳定 ID 和必要的计数、状态、耗时。超长详情以有效 JSON 标记截断，恢复历史时再次脱敏。完整约定见 [日志规范](docs/logging.md)。
- 组件健康按相同功能／模块条件筛选，展开查看调度运行次数、超时、最近错误和诊断事件；服务启停仍在模块管理。
